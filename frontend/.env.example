# URL of the backend API this frontend talks to.
# Local dev default (matches the backend's default port 8000):
NEXT_PUBLIC_API_URL=http://localhost:8000

# In production, point this at the deployed backend's own domain, e.g.:
# NEXT_PUBLIC_API_URL=https://backend-chi-virid-16.vercel.app
